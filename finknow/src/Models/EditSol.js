export default class EditSol
{
    constructor(sol_id,solution,user,qid)
    {
        this.solId=parseInt(sol_id);
        this.solution1=solution;
        this.username=user;
        this.queryId=qid;
    }
}
